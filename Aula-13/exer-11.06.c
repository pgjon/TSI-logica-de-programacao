/*
    Escreva um algoritmo para ler 2 valores A e B.Se A for igual a B devem ser lidos novos valores para A e B. Se A for menor que B calcular e imprimir a soma dos números ímpares existentes entre A (inclusive) e B (inclusive). Se A for maior que B calcular e imprimir a média aritmética dos múltiplos de 3existentes entre A (inclusive) e B (inclusive).OBS: Considere que só serão informados valores inteiros positivos.
    
    [Entrada]        | [Entrada]       | [Entrada]        | [Entrada]
    4 (A)  4 (B)     | 4 (A)  12 (B)   | 13 (A)  4 (B)    | 15 (A)  3 (B)
    4 (A) 11 (B)     |                 |                  |
                     |                 |                  |  
    [Saída]          | [Saída]         | [Saída]          | [Saída]
    32               | 32              | 9                | 9
*/
# include <stdio.h>
# include <locale.h>

int main(){
    setlocale(LC_ALL, "pt_BR.UTF-8");

    // Variables
    
    // Inputs

    // Processing

    // Outputs

    return 0;
}