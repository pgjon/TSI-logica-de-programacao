/*
    9.10) Uma sequência de valores é obtida com as seguintes regras:Sendo N um número da sequência, o próximo é calculado da seguinte forma:•Se N é ímpar o próximo é obtido da seguinte forma: 3*N + 1.•Se N é par o próximo é obtido da seguinte forma: N/2.•A sequência termina quando N é igual a 1.Escreva um algoritmo para ler uma quantidade indeterminada de inteiros. Para cada inteiro imprima a sequência gerada conforme as regras acima (considere que o valor lido é o primeiro número da sequência). O programa termina ao ser informado um inteiro igual a zero ou negativo.
    [Entrada]           [Saída]
    2                   2 1
    1                   1
    3                   3 10 5 16 8 4 2 1
    16                  16 8 4 2 1
    0
*/
# include <stdio.h>
# include <locale.h>

int main(){
    setlocale(LC_ALL, "pt_BR.UTF-8");

    // Variables
    
    // Inputs

    // Processing

    // Outputs

    return 0;
}